import Link from "next/link";
import { StorefrontFooter, StorefrontHeader } from "@/components/Storefront";
export default async function PaymentStatus({ params }: { params: Promise<{ status: string }> }) { const { status }=await params; const title=status === "sucesso" ? "Recebemos seu pagamento" : status === "pendente" ? "Seu pagamento está pendente" : "Não foi possível concluir o pagamento"; return <><StorefrontHeader /><main className="page container"><div className="auth panel"><p className="eyebrow">Pagamento</p><h1 style={{ marginTop:18,fontSize:42 }}>{title}</h1><p className="lead">A confirmação e a liberação são feitas pelo servidor após a notificação oficial do pagamento. Quando aprovado, seu material aparecerá em Minhas Compras.</p><Link className="button" href="/minhas-compras">Ir para Minhas Compras</Link></div></main><StorefrontFooter /></>; }
