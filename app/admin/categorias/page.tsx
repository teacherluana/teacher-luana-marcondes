import { createAdminSupabaseClient } from "@/lib/supabase/admin";
import { saveCategory } from "./actions";
export const dynamic="force-dynamic";
export default async function CategoriesPage(){const {data}=await createAdminSupabaseClient().from('categories').select('*').order('sort_order');return <><p className="eyebrow">Organização</p><h1 style={{marginTop:14,fontSize:46}}>Categorias</h1><form action={saveCategory} className="panel form" style={{marginTop:24}}><h2 style={{fontSize:26}}>Nova categoria</h2><div className="row"><label style={{flex:1}}>Nome<input className="input" name="name" required/></label><label style={{flex:1}}>Slug opcional<input className="input" name="slug"/></label></div><label>Descrição<input className="input" name="description"/></label><label><input type="checkbox" name="is_active" defaultChecked/> Exibir no catálogo</label><button className="button">Salvar categoria</button></form><div className="table-wrap" style={{marginTop:22}}><table className="table"><thead><tr><th>Nome</th><th>Slug</th><th>Status</th></tr></thead><tbody>{(data??[]).map(category=><tr key={category.id}><td>{category.name}</td><td>{category.slug}</td><td><span className="pill">{category.is_active?'Ativa':'Inativa'}</span></td></tr>)}</tbody></table></div></>;}
