import { createAdminSupabaseClient } from "@/lib/supabase/admin";
import { saveCoupon } from "./actions";
export const dynamic="force-dynamic";
export default async function CouponsPage(){const {data}=await createAdminSupabaseClient().from('coupons').select('*').order('created_at',{ascending:false});return <><p className="eyebrow">Promoções</p><h1 style={{marginTop:14,fontSize:46}}>Cupons</h1><form action={saveCoupon} className="panel form" style={{marginTop:24}}><h2 style={{fontSize:26}}>Novo cupom</h2><div className="row"><label style={{flex:1}}>Código<input className="input" name="code" required placeholder="AULA10"/></label><label>Tipo<select className="input" name="discount_type"><option value="percentage">Porcentagem</option><option value="fixed">Valor fixo</option></select></label><label>Desconto<input className="input" name="discount_value" type="number" min="0" required/></label></div><div className="row"><label style={{flex:1}}>Pedido mínimo (R$)<input className="input" name="min_order" type="number" step="0.01" min="0" defaultValue="0"/></label><label style={{flex:1}}>Limite de usos<input className="input" name="max_uses" type="number" min="1"/></label></div><label>Descrição<input className="input" name="description"/></label><button className="button">Criar cupom</button></form><div className="table-wrap" style={{marginTop:22}}><table className="table"><thead><tr><th>Código</th><th>Desconto</th><th>Usos</th><th>Status</th></tr></thead><tbody>{(data??[]).map(coupon=><tr key={coupon.id}><td><b>{coupon.code}</b></td><td>{coupon.discount_type==='percentage'?`${coupon.discount_value}%`:`R$ ${(coupon.discount_value/100).toFixed(2)}`}</td><td>{coupon.used_count}/{coupon.max_uses??'∞'}</td><td><span className="pill">{coupon.is_active?'Ativo':'Inativo'}</span></td></tr>)}</tbody></table></div></>;}
