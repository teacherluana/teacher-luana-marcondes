import { createAdminSupabaseClient } from "@/lib/supabase/admin";
import { formatCurrency, formatDate } from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function OrdersPage(){const {data}=await createAdminSupabaseClient().from('orders').select('id,order_number,status,total_cents,created_at,profiles(name,email),payments(provider_payment_id,status)').order('created_at',{ascending:false});return <><p className="eyebrow">Comercial</p><h1 style={{marginTop:14,fontSize:46}}>Pedidos</h1><div className="table-wrap" style={{marginTop:24}}><table className="table"><thead><tr><th>Pedido</th><th>Cliente</th><th>Total</th><th>Pagamento</th><th>Status</th><th>Data</th></tr></thead><tbody>{(data??[]).map(order=><tr key={order.id}><td>{order.order_number}</td><td>{(order.profiles as {name?:string;email?:string}|null)?.name||'—'}<br/><small>{(order.profiles as {email?:string}|null)?.email}</small></td><td>{formatCurrency(Number(order.total_cents))}</td><td>{(order.payments as unknown as {provider_payment_id?:string}|null)?.provider_payment_id??'Aguardando'}</td><td><span className="pill">{order.status}</span></td><td>{formatDate(order.created_at)}</td></tr>)}</tbody></table></div></>;}
