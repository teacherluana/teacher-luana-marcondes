import { notFound } from "next/navigation";
import { createAdminSupabaseClient } from "@/lib/supabase/admin";
import { ProductEditor } from "@/components/ProductEditor";
export const dynamic="force-dynamic";
export default async function EditProductPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const db=createAdminSupabaseClient();const [{data:product},{data:materials},{data:categories},{data:kit},{data:relations}]=await Promise.all([db.from('products').select('id,title,slug,description,short_description,regular_price_cents,sale_price_cents,product_kind,status,grade_level,material_type,tags').eq('id',id).single(),db.from('products').select('id,title').neq('status','archived').order('title'),db.from('categories').select('id,name').eq('is_active',true).order('sort_order'),db.from('kit_items').select('included_product_id').eq('kit_product_id',id),db.from('product_categories').select('category_id').eq('product_id',id)]);if(!product)notFound();return <><p className="eyebrow">Catálogo</p><h1 style={{marginTop:14,fontSize:46}}>Editar material</h1><div style={{marginTop:24}}><ProductEditor product={product} materials={materials??[]} categories={categories??[]} initialKitIds={(kit??[]).map(item=>item.included_product_id)} initialCategoryIds={(relations??[]).map(item=>item.category_id)}/></div></>;}
