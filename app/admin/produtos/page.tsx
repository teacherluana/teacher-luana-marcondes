import Link from "next/link";
import { createAdminSupabaseClient } from "@/lib/supabase/admin";
import { formatCurrency } from "@/lib/utils";
import { archiveProduct } from "./actions";
export const dynamic="force-dynamic";
export default async function AdminProducts(){const {data:products}=await createAdminSupabaseClient().from('products').select('id,title,status,product_kind,regular_price_cents,sale_price_cents,sales_count,updated_at').order('updated_at',{ascending:false});return <><div className="section-heading"><div><p>Catálogo</p><h1 style={{fontSize:46}}>Produtos e kits</h1></div><Link className="button" href="/admin/produtos/novo">Novo produto +</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Material</th><th>Tipo</th><th>Status</th><th>Preço</th><th>Vendas</th><th></th></tr></thead><tbody>{(products??[]).map((product)=><tr key={product.id}><td><b>{product.title}</b></td><td>{product.product_kind}</td><td><span className="pill">{product.status}</span></td><td>{formatCurrency(Number(product.sale_price_cents??product.regular_price_cents))}</td><td>{product.sales_count}</td><td><Link className="link" href={`/admin/produtos/${product.id}`}>Editar</Link><form action={archiveProduct} style={{display:'inline',marginLeft:12}}><input type="hidden" name="id" value={product.id}/><button className="link" type="submit">Arquivar</button></form></td></tr>)}</tbody></table></div></>;}
