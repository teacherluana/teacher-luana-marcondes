"use client";
import { useState } from "react";
import { StorefrontFooter, StorefrontHeader } from "@/components/Storefront";
export default function ContactPage(){const [message,setMessage]=useState('');return <><StorefrontHeader/><main className="page container"><div className="auth"><form className="form panel" onSubmit={async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});setMessage(response.ok?'Mensagem enviada. Obrigada!':'Não foi possível enviar agora.');}}><p className="eyebrow">Contato</p><h1 style={{fontSize:42}}>Fale com a Luana</h1><label>Nome<input className="input" name="name" required/></label><label>E-mail<input className="input" name="email" type="email" required/></label><label>Mensagem<textarea className="input" name="message" required minLength={10} style={{minHeight:130,padding:12}}/></label><button className="button">Enviar mensagem</button>{message?<p className="success">{message}</p>:null}</form></div></main><StorefrontFooter/></>;}
