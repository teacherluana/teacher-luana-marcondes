"use client";
import { useState } from "react";
import { AssetUploader } from "@/components/AssetUploader";
import { saveProduct } from "@/app/admin/produtos/actions";
type Material={id:string;title:string};type Category={id:string;name:string};type Product={id:string;title:string;slug:string;description:string;short_description:string|null;regular_price_cents:number;sale_price_cents:number|null;product_kind:string;status:string;grade_level:string|null;material_type:string|null;tags:string[]};
export function ProductEditor({product,materials,categories,initialKitIds=[],initialCategoryIds=[]}:{product:Product|null;materials:Material[];categories:Category[];initialKitIds?:string[];initialCategoryIds?:string[]}){const [kind,setKind]=useState(product?.product_kind??'individual');const [kitIds,setKitIds]=useState(initialKitIds);const [categoryIds,setCategoryIds]=useState(initialCategoryIds);const toggle=(id:string,setter:React.Dispatch<React.SetStateAction<string[]>>)=>setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id]);return <form action={saveProduct} className="panel form"><input name="id" type="hidden" value={product?.id??''}/><div className="row"><label style={{flex:1}}>Título<input required name="title" className="input" defaultValue={product?.title}/></label><label style={{flex:1}}>Slug opcional<input name="slug" className="input" defaultValue={product?.slug}/></label></div><label>Descrição<textarea required name="description" className="input" style={{minHeight:120,padding:12}} defaultValue={product?.description}/></label><label>Resumo curto<input name="short_description" className="input" defaultValue={product?.short_description??''}/></label><div className="row"><label style={{flex:1}}>Preço normal (R$)<input required min="0" step="0.01" name="regular_price" type="number" className="input" defaultValue={product?product.regular_price_cents/100:''}/></label><label style={{flex:1}}>Preço promocional (R$)<input min="0" step="0.01" name="sale_price" type="number" className="input" defaultValue={product?.sale_price_cents?product.sale_price_cents/100:''}/></label><label>Tipo<select name="product_kind" className="input" value={kind} onChange={event=>setKind(event.target.value)}><option value="individual">Material</option><option value="kit">Kit</option></select></label><label>Status<select name="status" className="input" defaultValue={product?.status??'draft'}><option value="draft">Rascunho</option><option value="published">Publicado</option></select></label></div><div className="row"><label style={{flex:1}}>Faixa/ano<input name="grade_level" className="input" defaultValue={product?.grade_level??''}/></label><label style={{flex:1}}>Tipo de atividade<input name="material_type" className="input" defaultValue={product?.material_type??''}/></label><label style={{flex:1}}>Tags (separadas por vírgula)<input name="tags" className="input" defaultValue={product?.tags?.join(', ')??''}/></label></div><fieldset style={{border:'1px solid var(--line)',borderRadius:16,padding:16}}><legend>Categorias</legend>{categories.map(category=><label key={category.id} style={{display:'inline-block',padding:'6px 14px 6px 0'}}><input type="checkbox" checked={categoryIds.includes(category.id)} onChange={()=>toggle(category.id,setCategoryIds)}/> {category.name}</label>)}<input type="hidden" name="category_ids" value={categoryIds.join(',')}/></fieldset>{kind==='kit'?<fieldset style={{border:'1px solid var(--line)',borderRadius:16,padding:16}}><legend>Materiais incluídos no kit</legend>{materials.filter(material=>material.id!==product?.id).map(material=><label key={material.id} style={{display:'block',padding:'6px 0'}}><input type="checkbox" checked={kitIds.includes(material.id)} onChange={()=>toggle(material.id,setKitIds)}/> {material.title}</label>)}<input type="hidden" name="kit_items" value={kitIds.join(',')}/></fieldset>:null}<button className="button" type="submit">Salvar produto</button>{product?<div className="stack" style={{borderTop:'1px solid var(--line)',paddingTop:18}}><h2 style={{fontSize:26}}>Arquivos privados</h2><AssetUploader productId={product.id} kind="cover" label="Upload de capa"/><AssetUploader productId={product.id} kind="preview" label="Upload de prévia"/><AssetUploader productId={product.id} kind="pdf" label="Upload do PDF"/></div>:<div className="notice">Salve o produto uma primeira vez para liberar o upload de capa, prévias e PDF.</div>}</form>}
